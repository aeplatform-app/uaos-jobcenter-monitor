# QA Local Only Test Plan

Local checks without external accounts:
- AI Factory safety check.
- Cost guard.
- Stage-specific plan checks.
- Tests when relevant.
- Controlled frontend build only when UI/build-sensitive files change.

No GitHub, Linear, Vercel, DNS, payment, or device services are contacted.

