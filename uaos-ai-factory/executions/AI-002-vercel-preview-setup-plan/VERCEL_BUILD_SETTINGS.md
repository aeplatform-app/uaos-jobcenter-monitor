# Vercel Build Settings

Future settings:
- Root directory: `uaos-live-clean`
- Install command: `npm install`
- Build command: `npm run build`
- Output directory: `dist`
- Framework preset: Vite
- Node version policy: use the version supported by project lockfiles and CI policy when finalized.
- Environment variables: none required unless later documented.

Build checks must not claim commercial readiness. UAOS remains prototype/demo/staging oriented.

