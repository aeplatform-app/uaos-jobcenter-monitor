# GitHub Review And Merge Policy

- No automatic merge to `master` or `main`.
- Merge to `develop` only after checks pass.
- Staging preview branch only after review.
- Production release requires a manual gate.

