# AI-014 Private Demo Gateway Config Safety Seal

LOCAL ONLY

NOT PUBLIC RELEASE

Status: DONE_LOCAL_PLAN_ONLY

The private demo gateway link/config changes are intentional local owner changes for Jobcenter/supporter screenshots only.

## Safety Seal

- The private gateway config changes are local-only.
- The changes support screenshots and private presentation context only.
- They are not a public release.
- They are not a production release.
- The Vercel config must not be executed.
- No push is allowed before GitHub transfer.
- No deploy command is allowed.
- No Vercel command is allowed.
- No payment flow is allowed.
- No real keyboard writer/export is allowed.

## Local Config Context

- `deploy/aeplatform-private-demo-gateway/vercel.json` was intentionally changed by the owner.
- `deploy/aeplatform-private-demo-gateway/vercel.broken.backup.json` is an intentional local backup context file.
- These files are committed only as a local safety-sealed snapshot.
