{
  "name": "UAOS Code X Fast Mode Rules",
  "purpose": "Reduce Code X slowness after update by limiting file reading and task scope.",
  "rules": [
    "Do not scan the whole repository.",
    "Do not read dist folders unless the task is build/deploy verification.",
    "Do not read backups unless explicitly requested.",
    "Do not read PDF/PPTX/ZIP binaries unless QA explicitly requires file existence/size.",
    "Do not inspect node_modules.",
    "Do not inspect .git.",
    "Do not inspect old generated reports unless named.",
    "Work only on explicitly listed files/folders.",
    "Prefer targeted checks over broad scans.",
    "Max one block at a time unless requested.",
    "Stop on first failure.",
    "Report if a task would require broad repo scan."
  ],
  "ignoreFoldersAndPatterns": [
    "node_modules/",
    ".git/",
    "uaos-live-clean/dist/",
    "backups/",
    "**/pdf-output/",
    "**/ppt-output/",
    "**/*.zip",
    "**/*.pdf",
    "**/*.pptx",
    "**/*.png",
    "**/*.jpg",
    "**/*.mp4",
    "**/*.wav",
    "**/*.mp3"
  ],
  "enforcement": {
    "scope": "explicit-files-and-folders-only",
    "failureMode": "stop-on-first-failure",
    "broadScanPolicy": "report-required-scope-before-continuing"
  }
}
