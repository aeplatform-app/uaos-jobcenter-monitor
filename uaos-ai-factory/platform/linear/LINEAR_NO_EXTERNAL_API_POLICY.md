# Linear No External API Policy

No Linear API, token, network action, webhook, or external automation is allowed in Stage 6.

This stage creates local planning files only. Any future Linear connection requires manual approval and a separate scoped task.

