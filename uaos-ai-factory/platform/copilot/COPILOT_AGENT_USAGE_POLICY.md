# Copilot Agent Usage Policy

Copilot Agent is planned for low/medium-risk issues after GitHub transfer and branch protection.

Rules:
- It must work only from issues.
- It must open branches and PRs.
- It must not merge to `master` / `main`.
- It must not deploy or enable payment/writer/export.
- Integration is planned only and not active.

