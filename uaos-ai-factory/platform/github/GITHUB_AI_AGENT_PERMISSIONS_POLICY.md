# GitHub AI Agent Permissions Policy

Future permissions:
- AI workers can open branches and PRs after approval.
- AI workers cannot merge to protected branches.
- AI workers cannot deploy production.
- AI workers cannot access payment/live secrets.
- AI workers cannot enable real writer/export.
- AI workers must use branch + PR workflow.

