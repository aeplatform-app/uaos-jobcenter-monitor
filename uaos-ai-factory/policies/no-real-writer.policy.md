# No Real Writer Policy

Real keyboard writer/output and real `.STY`, `.SET`, `.PRS`, `.STL`, `.PAT`, `.MSP`, or `.KST` export are blocked until explicit approval.

Allowed work:
- Safe readers.
- Metadata extraction.
- Hex/ASCII inspection.
- Adapter interfaces.
- Mock/demo exporters that cannot write to real devices or real keyboard formats.

