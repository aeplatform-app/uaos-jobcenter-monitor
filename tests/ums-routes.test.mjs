import test from "node:test";import assert from "node:assert/strict";import{createRequire}from"node:module";const require=createRequire(import.meta.url);const{sanitizeProject}=require("../backend/umsRoutes.cjs");test("forces private non-training projects",()=>{const p=sanitizeProject({id:"../unsafe",rights:{trainingAllowed:true},timeline:{sections:[{id:"intro/1",startMs:-1,endMs:1000}]}});assert.equal(p.rights.category,"user-private-project");assert.equal(p.rights.trainingAllowed,false);assert.equal(p.timeline.sections[0].startMs,0);assert.equal(p.timeline.sections[0].id.includes("/"),false);});