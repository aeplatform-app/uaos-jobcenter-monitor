export function validateUAOSProject(project){ const errors=[]; const warnings=[]; if(!project)errors.push("Project is empty"); if(project&&project.product!=="UAOS")errors.push("Not a UAOS project"); if(project&&!project.timeline&&!project.events)warnings.push("No timeline or events found"); return {ok:errors.length===0,errors,warnings}; }
export function validateUAOSStyle(style){ const errors=[]; const warnings=[]; if(!style)errors.push("Style is empty"); if(style&&style.product!=="UAOS")errors.push("Not a UAOS style"); if(style&&!style.patterns)errors.push("No patterns found"); return {ok:errors.length===0,errors,warnings}; }
