# UAOS Social Manual Handoff Readiness

Status: BLOCKED_MANUAL_HANDOFF
Tutorials: 140
Render manifests: 560
Draft queue items: 700
OAuth configured: 0/23

Required external actions:
- Install FFmpeg or render manually from the generated render manifests.
- Record or approve Arabic narration for every tutorial before final render approval.
- Configure official platform OAuth/API credentials and approved app scopes.
- Complete manual review and owner approval before any private upload, scheduling, or publication.

Safe local commands:
- npm run academy:status
- npm run academy:validate:all
- npm run academy:oauth:status
- npm run academy:render:sample
- npm run academy:render:status
- npm run academy:queue:dry-run

Publication allowed: false
Real network actions performed: false
